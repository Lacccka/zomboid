-- PackFlow runtime fingerprint. Keep this tiny and dependency-free so logs can
-- identify the exact deployed client copy before any sorting code runs.
print("[LCC PackFlow] build=0.7.13 runtime=client natural-sort=plain-v2 search-gate=off mass-transfer=vanilla-mp+visual-floor-v1 floor-source=on physical-trace=on tooltip-pass-through=v1")