# LCC B42.20 Vehicle Integration Bridge

Status: **BLOCKED_PENDING_PERMISSION**.

Preserves the current LCC PZK compatibility path shims byte-for-byte for local regression testing. The current audit treats the target extension policy as permission-required, so the active Workshop descriptor is intentionally absent.
