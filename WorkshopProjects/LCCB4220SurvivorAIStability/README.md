# LCC B42.20 Survivor AI Stability

Status: **BLOCKED_PENDING_PERMISSION**.

This project preserves the current Bandits fixes byte-for-byte for local regression testing, including dedicated-server guards, empty-server wanderer handling, zombie-square cache protection and farming action isolation.

It also contains the current full-file Bandits overrides. Those files are tracked by strict upstream contracts in `tools/compat_contracts.json`; therefore this project intentionally has no active `workshop.txt` and must not be uploaded until permission is documented or those overrides are replaced with a publication-safe implementation.
