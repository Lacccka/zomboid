# LCC B42.20 Vaccine API Bridge

Status: **READY_FOR_UNLISTED_TEST**.

Contains the existing LCC root-level `BodyLocations` compatibility shim and LCC Guard. The shim redirects the legacy import to the Build 42 vanilla module and does not contain zRe source/assets.
