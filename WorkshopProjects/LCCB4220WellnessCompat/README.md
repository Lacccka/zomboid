# LCC B42.20 Wellness Compatibility

Status: **BLOCKED_PENDING_PERMISSION**.

Preserves the current Lifestyle bath/shower compatibility and Yoga skill-panel integration byte-for-byte for local regression testing. The target mod's extension policy requires permission, so there is intentionally no active Workshop descriptor.

Lifestyle-specific Russian text is not bundled here; it is isolated in `LCCB4220ThirdPartyRU`.
